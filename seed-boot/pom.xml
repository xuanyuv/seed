<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>com.jadyer.seed</groupId>
		<artifactId>seed</artifactId>
		<version>1.1</version>
	</parent>
	<artifactId>seed-boot</artifactId>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
	</properties>

	<dependencies>
		<dependency>
			<groupId>com.jadyer.seed</groupId>
			<artifactId>seed-comm</artifactId>
			<version>${project.parent.version}</version>
		</dependency>
	</dependencies>

	<!--
	JSP页面测试时，要用到下面的配置，详见https://jadyer.github.io/2016/07/29/idea-springboot-jsp/
	-->
	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<mainClass>com.jadyer.seed.boot.BootStrap</mainClass>
				</configuration>
			</plugin>
		</plugins>
	</build>

	<!-- 打成jar时，配置成下面这样就可以了（mvn package命令执行时会触发这里的repackage，这样就会打成一个可执行的jar） -->
	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<mainClass>com.jadyer.seed.boot.BootRun</mainClass>
				</configuration>
				<executions>
					<execution>
						<goals>
							<goal>repackage</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
</project>