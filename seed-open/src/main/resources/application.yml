scan:
  base:
    packages: com.jadyer.seed.open

logging:
  path: /home/seed/log/open
  config: classpath:logback-open.xml

---
spring:
  profiles: local
server:
  port: 80

---
spring:
  profiles: prod
server:
  port: 8080